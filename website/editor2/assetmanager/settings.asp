<%
Dim arrBaseFolder
Redim arrBaseFolder(4)
Dim arrBaseName
Redim arrBaseName(4)

Dim bReturnAbsolute
bReturnAbsolute=false

arrBaseFolder(0)="/data/images/"'Use "Relative to Root" Path
arrBaseName(0)="Images"

arrBaseFolder(1)="/data/files/"
arrBaseName(1)="Files"


%>